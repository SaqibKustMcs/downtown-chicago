import 'package:flutter/material.dart';

import '../styles.dart';

class Shadows {
  static BoxShadow hallSelectionCardShadow = const BoxShadow(color: Color(0x40000000), blurRadius: 4, spreadRadius: 0, offset: Offset(0, 1));
}
